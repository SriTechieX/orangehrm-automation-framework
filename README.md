# orangehrm-automation-framework
The goal is to automate testing for the OrangeHRM web application using Playwright (a browser automation tool) and TypeScript (for structured, typed code). Playwright allows you to automate tasks in web browsers, which can be useful for testing applications to ensure that they function correctly
